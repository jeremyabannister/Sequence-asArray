import XCTest
@testable import Sequence_asArray

final class Sequence_asArrayTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Sequence_asArray().text, "Hello, World!")
    }
}
